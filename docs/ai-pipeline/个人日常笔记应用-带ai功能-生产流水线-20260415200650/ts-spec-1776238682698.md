# 技术规格（TS）：spec-1776238682698

## 元信息

- 流水线：个人日常笔记应用（带AI功能）-生产流水线
- 规格 ID：spec-1776238682698
- 关联 PRD：prd-1776235691782
- 状态：draft
- 更新时间：2026-04-15T12:00:33.026Z


---

# TS 正文（Markdown）

```json
{
  "TS": {
    "1. 技术栈": {
      "语言": "TypeScript 5+",
      "框架": {
        "后端": "Node.js + Express",
        "AI集成": "OpenAI API / 本地 ONNX Runtime",
        "数据库": "SQLite (本地) + PostgreSQL (云端)",
        "验证": "Zod",
        "测试": "Jest + Supertest"
      }
    },
    "2. 数据模型": {
      "Note": {
        "note_id": "string (UUID)",
        "user_id": "string (UUID)",
        "content": "string",
        "raw_content": {
          "text": "string | null",
          "image_paths": "string[] | null",
          "audio_path": "string | null"
        },
        "summary": "string | null",
        "keywords": "string[] | null",
        "suggested_tags": "string[] | null",
        "tags": "string[] | null",
        "status": "enum('draft', 'pending_ai', 'processed', 'ai_failed')",
        "ai_mode_used": "enum('cloud', 'local', 'none')",
        "create_time": "ISO8601 string",
        "update_time": "ISO8601 string",
        "metadata": {
          "word_count": "number",
          "has_image": "boolean",
          "has_audio": "boolean"
        }
      },
      "User": {
        "user_id": "string (UUID)",
        "preferences": {
          "default_ai_mode": "enum('cloud', 'local')",
          "auto_sync": "boolean"
        },
        "historical_tags": "string[]"
      },
      "SearchIndex": {
        "note_id": "string (UUID)",
        "content_vector": "number[]",
        "keywords_index": "string[]",
        "last_indexed": "ISO8601 string"
      }
    },
    "3. API": [
      {
        "路径": "POST /api/notes",
        "请求": {
          "headers": {
            "Authorization": "Bearer <token>"
          },
          "body": {
            "content": "string",
            "raw_content": {
              "image_paths": "string[] | null",
              "audio_path": "string | null"
            },
            "ai_mode": "enum('cloud', 'local')"
          }
        },
        "响应": {
          "success": {
            "note_id": "string",
            "status": "string",
            "create_time": "string"
          },
          "error": {
            "code": "string",
            "message": "string"
          }
        },
        "错误码": {
          "4001": "存储空间不足",
          "4002": "文件大小超限",
          "4010": "未授权",
          "5001": "AI服务不可用"
        }
      },
      {
        "路径": "POST /api/notes/:noteId/ai-process",
        "请求": {
          "headers": {
            "Authorization": "Bearer <token>"
          },
          "body": {
            "force_local": "boolean"
          }
        },
        "响应": {
          "success": {
            "summary": "string",
            "keywords": "string[]",
            "suggested_tags": "string[]",
            "status": "string"
          }
        }
      },
      {
        "路径": "PUT /api/notes/:noteId/ai-results",
        "请求": {
          "headers": {
            "Authorization": "Bearer <token>"
          },
          "body": {
            "action": "enum('accept', 'edit', 'ignore')",
            "summary": "string | null",
            "keywords": "string[] | null",
            "tags": "string[] | null"
          }
        }
      },
      {
        "路径": "GET /api/search",
        "请求": {
          "query": {
            "q": "string",
            "limit": "number (default: 50)"
          }
        },
        "响应": {
          "results": [
            {
              "note_id": "string",
              "title": "string",
              "summary_preview": "string",
              "tags": "string[]",
              "create_time": "string",
              "relevance_score": "number"
            }
          ]
        }
      }
    ],
    "4. 核心流程": {
      "创建笔记": [
        "1. validateInput(content, raw_content, ai_mode)",
        "2. checkStorageSpace()",
        "3. IF raw_content.has_image THEN ocrProcessAsync()",
        "4. IF raw_content.has_audio THEN asrProcessAsync()",
        "5. combineContent(text, ocrResult, asrResult) -> final_content",
        "6. IF final_content.length >= 50 AND ai_mode != 'none' THEN",
        "   status = 'pending_ai'",
        "   queueAITask(note_id, final_content, ai_mode)",
        "ELSE",
        "   status = 'draft'",
        "7. saveNote(note_id, final_content, status)",
        "8. return { note_id, status }"
      ],
      "AI处理": [
        "1. getNoteContent(note_id)",
        "2. IF content.length < 50 THEN return",
        "3. IF user.preferences.ai_mode == 'cloud' AND networkOk() THEN",
        "   aiService = cloudAIService",
        "ELSE",
        "   aiService = localAIService",
        "4. result = await aiService.process(content, user.historical_tags)",
        "5. IF result.success THEN",
        "   updateNote(note_id, {",
        "     summary: result.summary,",
        "     keywords: result.keywords,",
        "     suggested_tags: filterTags(result.tags, user.historical_tags),",
        "     status: 'processed'",
        "   })",
        "ELSE",
        "   updateNote(note_id, { status: 'ai_failed' })",
        "   logError(result.error)"
      ],
      "语义搜索": [
        "1. query = normalizeQuery(input)",
        "2. keywordResults = searchByKeywords(query, limit)",
        "3. semanticResults = searchBySemantic(query, limit)",
        "4. combinedResults = combineScores(keywordResults, semanticResults, weights=[0.4, 0.6])",
        "5. sortedResults = sortByScore(combinedResults)",
        "6. IF sortedResults.length == 0 THEN",
        "   return { results: [], suggestions: getRecentNotes(3) }",
        "ELSE",
        "   return { results: sortedResults.slice(0, limit) }"
      ]
    },
    "5. 异常处理": {
      "错误规则": [
        "存储错误: 检查设备存储，返回4001",
        "网络超时: AI云端请求>10秒，降级本地模型",
        "AI服务失败: 记录状态ai_failed，提供手动编辑入口",
        "输入验证: Zod schema验证，返回400",
        "认证失败: 返回401，使用本地模式"
      ]
    },
    "6. 测试用例": [
      {
        "场景": "创建文本笔记触发AI处理",
        "输入": {
          "content": "本周项目会：1. 确定V1.2版本需求。2. 前端延期，需协调资源。3. 下周二进行设计评审。",
          "ai_mode": "cloud"
        },
        "输出": {
          "status": "pending_ai",
          "后续状态": "processed",
          "验证": "summary.length ≈ content.length * 0.25"
        }
      },
      {
        "场景": "短文本跳过AI处理",
        "输入": {
          "content": "简短笔记",
          "ai_mode": "cloud"
        },
        "输出": {
          "status": "draft",
          "验证": "summary == null"
        }
      },
      {
        "场景": "网络异常降级本地AI",
        "模拟": "mock网络失败",
        "输入": {
          "content": "长文本内容...",
          "ai_mode": "cloud"
        },
        "输出": {
          "ai_mode_used": "local",
          "验证": "处理完成时间 < 3秒"
        }
      },
      {
        "场景": "语义搜索匹配",
        "输入": {
          "query": "如何优化大模型速度",
          "笔记内容": "今天读了一篇关于大模型推理优化的文章，提到KV缓存和量化的技术"
        },
        "输出": {
          "results.length": ">0",
          "验证": "relevance_score > 0.7"
        }
      },
      {
        "场景": "存储空间不足",
        "模拟": "mock存储检查失败",
        "输入": "任何创建请求",
        "输出": {
          "error_code": "4001",
          "验证": "阻止创建，返回明确错误"
        }
      }
    ]
  }
}
```

---

## Machine-Readable JSON（附录）

```json

```
