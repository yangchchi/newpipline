# 规格文档：spec-1775806193363

## 元信息

- 流水线：AI笔记本-生产流水线
- 规格 ID：spec-1775806193363
- 关联 PRD：prd-1775804312357
- 状态：draft
- 更新时间：2026-04-10T08:01:40.710Z

## 功能规格（FS）

### API 规范

无 API 定义

### UI 组件规范

无 UI 组件定义

### 交互逻辑

无交互定义

## 技术规格（TS）

### 数据库 Schema
```json
{}
```

### 系统架构



### 第三方集成

无

## Machine-Readable JSON
```json
{
  "fsMarkdown": "# FS\n\n## 1. 目标\n解决用户笔记存量增长后手动整理成本高、查找效率低的问题，通过AI能力实现笔记的自动分类与智能搜索，提升个人知识管理效率。成功标准：AI自动分类准确率≥90%；搜索结果准确率≥92%；用户平均单次查找笔记时间从5分钟降低到30秒以内。\n\n## 2. 角色与场景\n- 用户：个人注册用户\n- 使用场景：创建/导入笔记后自动整理；通过关键词或自然语言快速查找历史笔记。\n\n## 3. 功能\n### 功能点1：笔记创建与编辑\n- 输入：用户输入的笔记标题、文本内容、上传的图片/附件文件。\n- 规则：\n    - IF 用户点击“新建笔记”按钮 THEN 打开空白编辑页面。\n    - IF 用户输入内容 THEN 系统每2秒自动保存一次草稿。\n    - IF 用户退出编辑页面（关闭或返回）THEN 系统将草稿保存为正式笔记。\n    - IF 笔记标题为空 THEN 系统提取笔记正文前10个字符作为标题。\n    - IF 单笔记总大小（含附件）> 100MB THEN 阻止保存，并提示“单笔记大小不能超过100MB”。\n- 输出：一条保存成功的笔记记录，包含系统生成的唯一ID、标题、内容、创建时间、最后更新时间。\n- 验收标准：用户可成功创建包含文字和附件的笔记；无标题时自动生成标题；超过大小限制时明确阻止。\n\n### 功能点2：文件导入\n- 输入：用户从本地设备选择的一个或多个文件（支持.txt, .pdf, .docx, .jpg, .png格式）。\n- 规则：\n    - IF 用户点击“导入”按钮并选择文件 THEN 系统开始解析文件内容。\n    - IF 文件为图片（.jpg, .png）THEN 调用OCR服务识别图中文字，识别结果作为笔记内容。\n    - IF 文件为文本类（.txt, .pdf, .docx）THEN 直接提取文本内容作为笔记内容。\n    - IF 单次导入文件数量 > 50个 THEN 阻止本次操作，并提示“单次最多导入50个文件”。\n    - IF 文件解析成功 THEN 为每个文件创建一条新笔记，标题默认为文件名（不含扩展名）。\n- 输出：一个笔记列表，包含所有导入成功的新笔记。\n- 验收标准：支持批量导入不同格式文件并正确解析内容；超过数量限制时明确阻止。\n\n### 功能点3：手动分类管理\n- 输入：用户输入的新分类名称；用户对现有分类的拖拽排序、重命名或删除操作。\n- 规则：\n    - IF 用户点击“新建分类”并输入名称 THEN 系统检查分类名是否已存在。\n    - IF 分类名已存在 THEN 阻止创建，并提示“该分类名称已存在，请修改”。\n    - IF 分类名长度 > 20字符 THEN 阻止创建，并提示“分类名称不能超过20个字符”。\n    - IF 用户删除一个分类 THEN 将该分类下的所有笔记移动到“未分类”中。\n    - IF 用户拖拽笔记至目标分类 THEN 将该笔记归类至目标分类下。\n- 输出：更新后的用户分类列表及笔记与分类的归属关系。\n- 验收标准：用户可成功创建、重命名、删除分类，并可通过拖拽为笔记分类；重名和超长名称被明确阻止。\n\n### 功能点4：AI自动分类\n- 输入：新创建或导入的笔记内容（文本）。\n- 规则：\n    - IF 笔记保存成功 THEN 系统自动调用AI分类模型分析笔记内容。\n    - IF AI模型返回推荐分类（置信度≥阈值）THEN 在界面中向用户弹出推荐结果（如：“推荐归类至‘工作项目’”）。\n    - IF AI模型调用失败或未返回有效结果 THEN 将该笔记自动归入“未分类”，并在界面提示“AI分类暂时无法使用，已为你归档到未分类”。\n    - IF 用户确认AI推荐 THEN 系统将笔记移至推荐分类。\n    - IF 用户修改AI推荐 THEN 系统将笔记移至用户指定的分类。\n- 输出：笔记的分类归属状态更新；向用户展示的AI推荐结果及确认界面。\n- 验收标准：新笔记保存后触发AI分类推荐；用户可一键确认或修改；AI服务异常时有明确降级处理。\n\n### 功能点5：语义与关键词搜索\n- 输入：用户在顶部搜索框输入的查询字符串（关键词或自然语言问句）。\n- 规则：\n    - IF 用户输入查询字符串并按下回车或点击搜索图标 THEN 系统并行执行以下操作：\n        1.  传统关键词匹配：在笔记标题和正文中进行关键词匹配。\n        2.  语义搜索：将查询字符串和笔记内容向量化，计算语义相似度。\n    - IF 搜索完成 THEN 系统融合两种搜索结果，按相关度（语义相似度权重70%+关键词匹配权重30%）排序，再按更新时间倒序二次排序。\n    - IF 总响应时间 > 500ms THEN 记录性能日志，但正常返回结果。\n    - IF 无任何匹配结果 THEN 返回空列表，并显示提示“暂无相关笔记，换个关键词试试吧”。\n- 输出：一个按规则排序的笔记结果列表。\n- 验收标准：输入自然语言（如“去年Q3的客户预算”）能返回相关笔记；搜索结果在500ms内返回；无结果时有明确提示。\n\n### 功能点6：搜索结果筛选\n- 输入：用户在搜索结果页点击“筛选”后选择的条件（分类、创建时间范围、文件类型）。\n- 规则：\n    - IF 用户应用筛选条件 THEN 系统在当前搜索结果列表的基础上进行过滤。\n    - IF 筛选条件为“分类” THEN 只显示属于所选分类的笔记。\n    - IF 筛选条件为“创建时间范围” THEN 只显示在该时间范围内创建的笔记。\n    - IF 筛选条件为“文件类型” THEN 只显示包含对应类型附件（或无附件）的笔记。\n    - IF 应用筛选后结果为空 THEN 显示“当前筛选条件下无笔记”。\n- 输出：一个经过筛选的笔记结果子集。\n- 验收标准：用户可基于分类、时间、文件类型对搜索结果进行二次过滤。\n\n## 4. 规则补充\n- 全局业务规则（if-then）\n    - IF 用户网络连接中断 THEN 系统将用户未保存的编辑内容缓存在本地，并提示“当前网络不佳，内容已本地缓存，网络恢复后自动同步”。\n    - IF 用户网络恢复 THEN 系统自动将本地缓存的草稿或待同步笔记上传至服务器。\n    - IF 用户删除笔记或分类 THEN 数据进入回收站，保留30天，30天后自动永久删除。\n\n## 5. 示例（必须）\n### 正常：\n**场景：AI自动分类与搜索**\n- Input:\n    1.  用户创建新笔记，标题为“Q3产品复盘”，内容包含“销售额”、“用户增长”、“市场策略”等。\n    2.  用户搜索“上个季度业绩总结”。\n- Output:\n    1.  笔记保存后，系统弹出提示：“AI推荐将本笔记归类至「工作汇报」”，用户点击确认。\n    2.  搜索后，包含“Q3产品复盘”的笔记出现在结果列表前列。\n\n### 异常：\n**场景：创建重复分类与搜索无结果**\n- Input:\n    1.  用户尝试创建一个名为“学习”的新分类，但该分类已存在。\n    2.  用户搜索一个不存在的关键词“火星殖民计划”。\n- Output:\n    1.  界面提示：“该分类名称已存在，请修改”，且分类未创建。\n    2.  结果显示为空，并显示提示语：“暂无相关笔记，换个关键词试试吧”。",
  "tsMarkdown": "# TS\n\n## 1. 技术栈\n- 语言：TypeScript 5+\n- 框架：Node.js + Express\n\n## 2. 数据模型\n```typescript\n// 数据模型定义\ninterface Note {\n    id: string; // UUID\n    title: string;\n    content: string;\n    attachments: Attachment[];\n    categoryId: string | null; // 关联Category.id\n    fileType: 'text' | 'image' | 'pdf' | 'docx' | 'none';\n    createdAt: Date;\n    updatedAt: Date;\n    isDeleted: boolean;\n    deletedAt: Date | null;\n}\n\ninterface Attachment {\n    id: string;\n    noteId: string;\n    filename: string;\n    fileSize: number; // bytes\n    mimeType: string;\n    storagePath: string;\n}\n\ninterface Category {\n    id: string;\n    name: string; // 最大20字符\n    userId: string;\n    order: number;\n    createdAt: Date;\n}\n\ninterface User {\n    id: string;\n    email: string;\n    // ... 其他用户字段\n}\n\ninterface SearchResult {\n    note: Note;\n    relevanceScore: number; // 0-1\n    keywordMatches: number;\n    semanticSimilarity: number;\n}\n```\n\n## 3. API\n\n### 3.1 笔记创建\n- 路径：`POST /api/notes`\n- 请求：\n```typescript\ninterface CreateNoteRequest {\n    title?: string;\n    content: string;\n    attachments?: File[];\n}\n```\n- 响应：\n```typescript\ninterface NoteResponse {\n    id: string;\n    title: string;\n    content: string;\n    categoryId: string | null;\n    createdAt: string;\n    updatedAt: string;\n}\n```\n- 错误码：\n  - 400: 请求数据无效\n  - 413: 笔记大小超过100MB\n  - 500: 服务器内部错误\n\n### 3.2 文件导入\n- 路径：`POST /api/notes/import`\n- 请求：`multipart/form-data`，文件数组\n- 响应：\n```typescript\ninterface ImportNotesResponse {\n    notes: NoteResponse[];\n    failedFiles: Array<{\n        filename: string;\n        reason: string;\n    }>;\n}\n```\n- 错误码：\n  - 400: 文件数量超过50个\n  - 415: 不支持的文件格式\n  - 500: OCR/解析失败\n\n### 3.3 分类管理\n- 路径：`POST /api/categories`\n- 请求：\n```typescript\ninterface CreateCategoryRequest {\n    name: string;\n}\n```\n- 响应：\n```typescript\ninterface CategoryResponse {\n    id: string;\n    name: string;\n    order: number;\n}\n```\n- 错误码：\n  - 400: 分类名已存在或超长\n  - 409: 分类名已存在\n\n### 3.4 AI分类推荐\n- 路径：`POST /api/notes/:noteId/ai-classify`\n- 请求：无body\n- 响应：\n```typescript\ninterface AIClassifyResponse {\n    recommendedCategoryId: string | null;\n    confidence: number; // 0-1\n    fallbackReason?: string;\n}\n```\n- 错误码：\n  - 404: 笔记不存在\n  - 503: AI服务不可用\n\n### 3.5 搜索笔记\n- 路径：`GET /api/search`\n- 请求：\n```typescript\ninterface SearchRequest {\n    query: string;\n    categoryId?: string;\n    startDate?: string;\n    endDate?: string;\n    fileType?: Note['fileType'];\n    limit?: number;\n    offset?: number;\n}\n```\n- 响应：\n```typescript\ninterface SearchResponse {\n    results: SearchResult[];\n    total: number;\n    queryTime: number; // ms\n}\n```\n- 错误码：\n  - 400: 查询参数无效\n  - 408: 查询超时\n\n## 4. 核心流程\n\n### 4.1 笔记创建与自动保存\n```typescript\n// 伪代码\nclass NoteService {\n    async createNote(request: CreateNoteRequest): Promise<NoteResponse> {\n        // 1. 验证大小\n        if (calculateTotalSize(request) > 100 * 1024 * 1024) {\n            throw new Error('单笔记大小不能超过100MB');\n        }\n        \n        // 2. 生成标题\n        const title = request.title || extractFirstChars(request.content, 10);\n        \n        // 3. 保存笔记\n        const note = await NoteRepository.create({\n            title,\n            content: request.content,\n            fileType: determineFileType(request.attachments)\n        });\n        \n        // 4. 触发AI分类（异步）\n        this.triggerAIClassification(note.id);\n        \n        // 5. 返回响应\n        return mapToNoteResponse(note);\n    }\n    \n    private triggerAIClassification(noteId: string): void {\n        // 异步任务\n        setTimeout(async () => {\n            try {\n                const result = await AIService.classifyNote(noteId);\n                if (result.confidence >= 0.9) {\n                    NotificationService.notifyUser(noteId, result);\n                }\n            } catch (error) {\n                // 降级处理\n                await NoteRepository.updateCategory(noteId, 'uncategorized');\n                NotificationService.notifyFallback(noteId);\n            }\n        }, 0);\n    }\n}\n```\n\n### 4.2 搜索流程\n```typescript\nclass SearchService {\n    async search(request: SearchRequest): Promise<SearchResponse> {\n        const startTime = Date.now();\n        \n        // 并行执行搜索\n        const [keywordResults, semanticResults] = await Promise.all([\n            this.keywordSearch(request.query),\n            this.semanticSearch(request.query)\n        ]);\n        \n        // 融合结果\n        const merged = this.mergeResults(keywordResults, semanticResults);\n        \n        // 应用筛选\n        const filtered = this.applyFilters(merged, request);\n        \n        // 排序\n        const sorted = this.sortResults(filtered);\n        \n        // 分页\n        const paginated = this.paginate(sorted, request.limit, request.offset);\n        \n        const queryTime = Date.now() - startTime;\n        if (queryTime > 500) {\n            PerformanceLogger.logSlowSearch(queryTime, request.query);\n        }\n        \n        return {\n            results: paginated,\n            total: sorted.length,\n            queryTime\n        };\n    }\n    \n    private mergeResults(keyword: SearchResult[], semantic: SearchResult[]): SearchResult[] {\n        // 按权重计算相关度分数\n        // 语义相似度权重70%，关键词匹配权重30%\n        const mergedMap = new Map<string, SearchResult>();\n        \n        // 合并逻辑...\n        return Array.from(mergedMap.values());\n    }\n}\n```\n\n## 5. 异常处理\n```typescript\n// 错误规则\nconst ErrorRules = {\n    NETWORK_ERROR: {\n        condition: () => !navigator.onLine,\n        action: () => {\n            LocalStorage.cacheDraft();\n            showNotification('当前网络不佳，内容已本地缓存，网络恢复后自动同步');\n        }\n    },\n    AI_SERVICE_UNAVAILABLE: {\n        condition: (error) => error.code === 'ECONNREFUSED',\n        action: (noteId) => {\n            NoteRepository.updateCategory(noteId, 'uncategorized');\n            showNotification('AI分类暂时无法使用，已为你归档到未分类');\n        }\n    },\n    FILE_SIZE_EXCEEDED: {\n        condition: (size) => size > 100 * 1024 * 1024,\n        action: () => {\n            throw new HttpError(413, '单笔记大小不能超过100MB');\n        }\n    }\n};\n```\n\n## 6. 测试用例\n\n### 测试1: 笔记创建-自动标题生成\n- 输入：\n```typescript\n{\n    title: \"\",\n    content: \"这是笔记内容的前10个字符测试\",\n    attachments: []\n}\n```\n- 输出：\n```typescript\n{\n    id: \"note_123\",\n    title: \"这是笔记内容\",\n    content: \"这是笔记内容的前10个字符测试\",\n    categoryId: null,\n    createdAt: \"2024-01-01T00:00:00Z\",\n    updatedAt: \"2024-01-01T00:00:00Z\"\n}\n```\n\n### 测试2: 文件导入-数量限制\n- 输入：51个.txt文件\n- 输出：\n```typescript\n{\n    error: {\n        code: 400,\n        message: \"单次最多导入50个文件\"\n    }\n}\n```\n\n### 测试3: AI分类-高置信度推荐\n- 输入：笔记内容包含\"销售额、用户增长、市场策略\"\n- 输出：\n```typescript\n{\n    recommendedCategoryId: \"cat_work_report\",\n    confidence: 0.95,\n    fallbackReason: null\n}\n```\n\n### 测试4: 搜索-自然语言查询\n- 输入：`query: \"上个季度业绩总结\"`\n- 输出：\n```typescript\n{\n    results: [\n        {\n            note: {\n                id: \"note_123\",\n                title: \"Q3产品复盘\",\n                content: \"销售额、用户增长、市场策略...\",\n                categoryId: \"cat_work_report\"\n            },\n            relevanceScore: 0.88,\n            keywordMatches: 1,\n            semanticSimilarity: 0.92\n        }\n    ],\n    total: 1,\n    queryTime: 350\n}\n```\n\n### 测试5: 创建重复分类\n- 输入：`{ name: \"学习\" }` (已存在)\n- 输出：\n```typescript\n{\n    error: {\n        code: 409,\n        message: \"该分类名称已存在，请修改\"\n    }\n}\n```",
  "functionalSpec": {
    "apis": [],
    "uiComponents": [],
    "interactions": []
  },
  "technicalSpec": {
    "databaseSchema": {},
    "architecture": "",
    "thirdPartyIntegrations": []
  },
  "organizationCodingSpec": {
    "orgName": "默认组织",
    "language": "typescript",
    "constraints": {
      "enabled": true,
      "testing": [
        "核心逻辑单测"
      ],
      "language": "typescript",
      "forbidden": [
        "滥用断言"
      ],
      "toolchain": [
        "TS 5+"
      ],
      "mustFollow": [
        "DTO 分离"
      ],
      "styleGuide": [
        "strict"
      ],
      "displayName": "TypeScript"
    }
  }
}
```
